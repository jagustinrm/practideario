// import { ToDo } from "./ToDo";

// interface Task {
//     title: string;
//     id: string;
// }

// interface AllToDoProps {
//     tasks: Task[];
// }

// export function AllToDo({tasks}: AllToDoProps) {
// return (
//     <>
//      {tasks.length ? (
//                tasks.map((task) => (

//                     <ToDo id={task.id} title={task.title}   />
                
//                 ))
//             ) : (
//                 <p>No hay tareas</p>
//             )}
//     </>
// )
// }